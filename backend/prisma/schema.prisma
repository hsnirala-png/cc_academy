generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

enum Role {
  ADMIN
  STUDENT
}

enum ClassMode {
  ONLINE
  OFFLINE
  HYBRID
}

enum SubscriptionStatus {
  ACTIVE
  EXPIRED
  CANCELLED
  PENDING
}

enum ExamType {
  PSTET_1
  PSTET_2
}

enum StreamChoice {
  SCIENCE_MATH
  SOCIAL_STUDIES
}

enum QuestionLanguage {
  PUNJABI
  ENGLISH
  HINDI
  BILINGUAL
}

enum MockSubject {
  PUNJABI
  ENGLISH
  CHILD_PEDAGOGY
  MATHS
  EVS
  MATHS_EVS
  SCIENCE_MATH
  SOCIAL_STUDIES
}

enum MockOption {
  A
  B
  C
  D
}

enum AttemptStatus {
  IN_PROGRESS
  SUBMITTED
}

enum ReferralPayoutMethodType {
  BANK
  UPI
}

enum ReferralWithdrawalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum ReferralTransactionType {
  REFERRAL_BONUS
  PRODUCT_PURCHASE
  WITHDRAWAL
}

enum ContactConversationStatus {
  OPEN
  REPLIED
  CLOSED
}

enum ContactSenderType {
  VISITOR
  ADMIN
}

enum TuitionParseStatus {
  UPLOADED
  PROCESSING
  PARSED
  NEEDS_REVIEW
  CONFIRMED
  FAILED
}

enum TuitionSessionStatus {
  ACTIVE
  PAUSED
  COMPLETED
  ARCHIVED
}

enum TuitionProgressStatus {
  NOT_STARTED
  IN_PROGRESS
  COMPLETED
}

enum TuitionSpeedMode {
  SLOW
  NORMAL
  FAST
}

enum TuitionDifficultyMode {
  EASY
  MEDIUM
  HARD
}

enum TuitionHomeworkStatus {
  GENERATED
  ASSIGNED
  SUBMITTED
  EVALUATED
}

model User {
  id           String   @id @default(cuid())
  name         String
  mobile       String   @unique
  email        String?  @unique
  state        String
  city         String
  passwordHash String
  role         Role     @default(STUDENT)
  mockTestsCreated MockTest[] @relation("MockTestsCreatedBy")
  productsCreated Product[] @relation("ProductsCreatedBy")
  referralCode String? @unique
  referrerId String?
  referrer User? @relation("UserReferrals", fields: [referrerId], references: [id], onDelete: SetNull)
  referredUsers User[] @relation("UserReferrals")
  enrollments  Enrollment[]
  lessonProgress LessonProgress[]
  attempts     Attempt[]
  subscriptions StudentSubscription[]
  referralPayoutMethods ReferralPayoutMethod[]
  referralTransactions ReferralTransaction[]
  referralWithdrawals ReferralWithdrawal[]
  productPurchases ProductPurchase[]
  contactConversations ContactConversation[]
  tuitionProfile TuitionProfile?
  tuitionUploads TuitionSyllabusUpload[]
  tuitionSyllabi TuitionSyllabus[]
  tuitionSessions TuitionSession[]
  tuitionProgress TuitionProgress[]
  tuitionHomework TuitionHomework[]
  tuitionHomeworkSubmissions TuitionHomeworkSubmission[]
  tuitionLessonDoubts TuitionLessonDoubt[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([referrerId])
  @@index([referralCode])
}

model Course {
  id          String       @id @default(cuid())
  title       String
  description String?      @db.Text
  isActive    Boolean      @default(true)
  chapters    Chapter[]
  enrollments Enrollment[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  @@index([isActive, title])
}

model Chapter {
  id          String    @id @default(cuid())
  courseId    String
  title       String
  description String?   @db.Text
  subSubject  StreamChoice?
  orderIndex  Int
  course      Course    @relation(fields: [courseId], references: [id], onDelete: Cascade)
  lessons     Lesson[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@unique([courseId, orderIndex])
  @@index([courseId, title])
}

model Lesson {
  id               String          @id @default(cuid())
  chapterId        String
  title            String
  orderIndex       Int
  videoUrl         String
  transcriptUrl    String?
  transcriptText   String?         @db.LongText
  transcriptSegments Json?
  audioUrl         String?
  audioDurationMs  Int?
  audioGeneratedAt DateTime?
  audioStatus      String?
  audioVoice       String?         @default("marin")
  audioLanguageHint String?
  durationSec      Int             @default(0)
  assessmentTestId String?
  chapter          Chapter         @relation(fields: [chapterId], references: [id], onDelete: Cascade)
  assessmentTest   MockTest?       @relation(fields: [assessmentTestId], references: [id], onDelete: SetNull)
  progress         LessonProgress[]
  createdAt        DateTime        @default(now())
  updatedAt        DateTime        @updatedAt

  @@unique([chapterId, orderIndex])
  @@index([chapterId, title])
  @@index([assessmentTestId])
}

model Enrollment {
  id         String   @id @default(cuid())
  userId     String
  courseId   String
  enrolledAt DateTime @default(now())
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  course     Course   @relation(fields: [courseId], references: [id], onDelete: Cascade)

  @@unique([userId, courseId])
  @@index([courseId])
}

model LessonProgress {
  id              String   @id @default(cuid())
  userId          String
  lessonId        String
  lastPositionSec Int      @default(0)
  completed       Boolean  @default(false)
  completedAt     DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  lesson          Lesson   @relation(fields: [lessonId], references: [id], onDelete: Cascade)

  @@unique([userId, lessonId])
  @@index([lessonId, completed])
}

model Product {
  id            String   @id @default(cuid())
  title         String
  examCategory  String
  examName      String
  courseType    String
  languageMode  String?
  thumbnailUrl  String?
  description   String?  @db.Text
  listPrice     Decimal  @db.Decimal(10, 2)
  salePrice     Decimal  @db.Decimal(10, 2)
  referralBonusAmount Decimal @default(0) @db.Decimal(10, 2)
  referralDiscountAmount Decimal @default(0) @db.Decimal(10, 2)
  accessDays    Int
  validityLabel String?
  addons        Json?
  isActive      Boolean  @default(true)
  createdBy     String?
  createdByUser User?    @relation("ProductsCreatedBy", fields: [createdBy], references: [id], onDelete: SetNull)
  purchases     ProductPurchase[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([isActive, examCategory, examName, courseType, languageMode])
  @@index([createdBy])
}

model CoachingClass {
  id           String   @id @default(cuid())
  name         String
  description  String?
  instructor   String?
  mode         ClassMode @default(ONLINE)
  startDate    DateTime
  endDate      DateTime?
  seats        Int?
  isActive     Boolean   @default(true)
  subscriptions StudentSubscription[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model SubscriptionPlan {
  id            String   @id @default(cuid())
  title         String
  description   String?
  price         Decimal  @db.Decimal(10, 2)
  durationDays  Int
  isActive      Boolean  @default(true)
  subscriptions StudentSubscription[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model StudentSubscription {
  id         String             @id @default(cuid())
  userId     String
  planId     String
  classId    String?
  startsOn   DateTime           @default(now())
  endsOn     DateTime?
  status     SubscriptionStatus @default(ACTIVE)
  user       User               @relation(fields: [userId], references: [id], onDelete: Cascade)
  plan       SubscriptionPlan   @relation(fields: [planId], references: [id], onDelete: Restrict)
  classItem  CoachingClass?     @relation(fields: [classId], references: [id], onDelete: SetNull)
  createdAt  DateTime           @default(now())
  updatedAt  DateTime           @updatedAt

  @@index([userId])
  @@index([planId])
  @@index([classId])
}

model MockTest {
  id          String       @id @default(cuid())
  title       String
  examType    ExamType
  subject     MockSubject
  streamChoice StreamChoice?
  languageMode QuestionLanguage?
  isActive    Boolean      @default(true)
  createdBy   String
  createdByUser User       @relation("MockTestsCreatedBy", fields: [createdBy], references: [id], onDelete: Restrict)
  questions   Question[]
  sections    MockTestSection[]
  attempts    Attempt[]
  linkedLessons Lesson[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  @@index([examType, subject, streamChoice, isActive])
  @@index([createdBy])
}

model MockTestSection {
  id            String    @id @default(cuid())
  mockTestId    String
  sectionLabel  String    @db.VarChar(120)
  sectionType   String    @default("GENERAL_MCQ") @db.VarChar(60)
  transcriptText String?  @db.LongText
  audioUrl      String?   @db.VarChar(512)
  questionLimit Int       @default(10)
  sortOrder     Int       @default(0)
  isActive      Boolean   @default(true)
  mockTest      MockTest  @relation(fields: [mockTestId], references: [id], onDelete: Cascade)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  @@unique([mockTestId, sectionLabel])
  @@index([mockTestId, sortOrder, isActive])
}

model Question {
  id           String      @id @default(cuid())
  mockTestId   String
  questionText String      @db.Text
  questionTextAlt String?  @db.Text
  optionA      String      @db.Text
  optionAAlt   String?     @db.Text
  optionB      String      @db.Text
  optionBAlt   String?     @db.Text
  optionC      String      @db.Text
  optionCAlt   String?     @db.Text
  optionD      String      @db.Text
  optionDAlt   String?     @db.Text
  correctOption MockOption
  explanation  String?     @db.Text
  explanationAlt String?   @db.Text
  sectionLabel String?     @db.VarChar(120)
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  mockTest     MockTest    @relation(fields: [mockTestId], references: [id], onDelete: Cascade)
  attemptQuestions AttemptQuestion[]
  attemptAnswers AttemptAnswer[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([mockTestId, isActive])
  @@index([mockTestId, displayOrder])
  @@index([mockTestId, sectionLabel, isActive])
}

model Attempt {
  id            String        @id @default(cuid())
  userId        String
  mockTestId    String
  status        AttemptStatus @default(IN_PROGRESS)
  totalQuestions Int
  correctCount  Int?
  scorePercent  Float?
  remarkText    String?
  startedAt     DateTime      @default(now())
  submittedAt   DateTime?
  user          User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  mockTest      MockTest      @relation(fields: [mockTestId], references: [id], onDelete: Restrict)
  attemptQuestions AttemptQuestion[]
  answers       AttemptAnswer[]

  @@index([userId, startedAt])
  @@index([mockTestId, startedAt])
  @@index([status, submittedAt])
}

model AttemptQuestion {
  id         String   @id @default(cuid())
  attemptId  String
  questionId String
  orderIndex Int
  attempt    Attempt  @relation(fields: [attemptId], references: [id], onDelete: Cascade)
  question   Question @relation(fields: [questionId], references: [id], onDelete: Restrict)

  @@unique([attemptId, questionId])
  @@unique([attemptId, orderIndex])
  @@index([questionId])
}

model AttemptAnswer {
  id            String     @id @default(cuid())
  attemptId      String
  questionId     String
  selectedOption MockOption
  answeredAt     DateTime   @default(now())
  attempt        Attempt    @relation(fields: [attemptId], references: [id], onDelete: Cascade)
  question       Question   @relation(fields: [questionId], references: [id], onDelete: Restrict)

  @@unique([attemptId, questionId])
  @@index([questionId])
}

model ProductPurchase {
  id            String   @id @default(cuid())
  userId        String
  productId     String
  amountPaid    Decimal  @db.Decimal(10, 2)
  walletUsed    Decimal  @db.Decimal(10, 2)
  referralBonusCredited Decimal @default(0) @db.Decimal(10, 2)
  createdAt     DateTime @default(now())
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  product       Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  referralTransactions ReferralTransaction[]

  @@index([userId, createdAt])
  @@index([productId, createdAt])
}

model ReferralPayoutMethod {
  id           String                   @id @default(cuid())
  userId       String
  type         ReferralPayoutMethodType
  bankName     String?
  accountNo    String?
  ifsc         String?
  place        String?
  upiId        String?
  isVerified   Boolean                  @default(false)
  verifiedAt   DateTime?
  verifiedBy   String?
  createdAt    DateTime                 @default(now())
  updatedAt    DateTime                 @updatedAt
  user         User                     @relation(fields: [userId], references: [id], onDelete: Cascade)
  withdrawals  ReferralWithdrawal[]

  @@index([userId, createdAt])
  @@index([isVerified, type])
}

model ReferralWithdrawal {
  id             String                    @id @default(cuid())
  userId         String
  payoutMethodId String
  amount         Decimal                   @db.Decimal(10, 2)
  status         ReferralWithdrawalStatus  @default(PENDING)
  requestedAt    DateTime                  @default(now())
  reviewedAt     DateTime?
  reviewedBy     String?
  adminNote      String?
  createdAt      DateTime                  @default(now())
  updatedAt      DateTime                  @updatedAt
  user           User                      @relation(fields: [userId], references: [id], onDelete: Cascade)
  payoutMethod   ReferralPayoutMethod      @relation(fields: [payoutMethodId], references: [id], onDelete: Restrict)
  referralTransactions ReferralTransaction[]

  @@index([userId, createdAt])
  @@index([status, createdAt])
}

model ReferralTransaction {
  id            String                 @id @default(cuid())
  userId        String
  amount        Decimal                @db.Decimal(10, 2)
  type          ReferralTransactionType
  description   String?
  purchaseId    String?
  withdrawalId  String?
  createdAt     DateTime               @default(now())
  user          User                   @relation(fields: [userId], references: [id], onDelete: Cascade)
  purchase      ProductPurchase?       @relation(fields: [purchaseId], references: [id], onDelete: SetNull)
  withdrawal    ReferralWithdrawal?    @relation(fields: [withdrawalId], references: [id], onDelete: SetNull)

  @@index([userId, createdAt])
  @@index([type, createdAt])
  @@index([purchaseId])
  @@index([withdrawalId])
}

model ContactConversation {
  id                String                    @id @default(cuid())
  userId            String?
  name              String
  email             String
  sourcePage        String?
  sourceUrl         String?                   @db.VarChar(1000)
  status            ContactConversationStatus @default(OPEN)
  latestMessageText String?                   @db.VarChar(500)
  repliedAt         DateTime?
  closedAt          DateTime?
  user              User?                     @relation(fields: [userId], references: [id], onDelete: SetNull)
  messages          ContactMessage[]
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt

  @@index([status, updatedAt])
  @@index([email, createdAt])
  @@index([userId, updatedAt])
}

model ContactMessage {
  id             String            @id @default(cuid())
  conversationId String
  senderType     ContactSenderType
  senderName     String
  senderEmail    String?
  body           String            @db.LongText
  createdAt      DateTime          @default(now())
  conversation   ContactConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)

  @@index([conversationId, createdAt])
}

model TuitionBoard {
  id        String           @id @default(cuid())
  code      String           @unique
  name      String
  isActive  Boolean          @default(true)
  profiles  TuitionProfile[]
  syllabi   TuitionSyllabus[]
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt

  @@index([isActive, name])
}

model TuitionSubject {
  id        String           @id @default(cuid())
  code      String           @unique
  name      String
  isActive  Boolean          @default(true)
  profiles  TuitionProfile[]
  syllabi   TuitionSyllabus[]
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt

  @@index([isActive, name])
}

model TuitionProfile {
  id                String                  @id @default(cuid())
  userId            String                  @unique
  boardId           String?
  classLevel        Int?
  subjectId         String?
  preferredLanguage String?
  activeSyllabusId  String?
  user              User                    @relation(fields: [userId], references: [id], onDelete: Cascade)
  board             TuitionBoard?           @relation(fields: [boardId], references: [id], onDelete: SetNull)
  subject           TuitionSubject?         @relation(fields: [subjectId], references: [id], onDelete: SetNull)
  activeSyllabus    TuitionSyllabus?        @relation("ActiveTuitionSyllabus", fields: [activeSyllabusId], references: [id], onDelete: SetNull)
  uploads           TuitionSyllabusUpload[]
  syllabi           TuitionSyllabus[]       @relation("ProfileTuitionSyllabi")
  chapterPlans      TuitionChapterPlan[]
  sessions          TuitionSession[]
  homework          TuitionHomework[]
  lessonCaches      TuitionLessonCache[]
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt

  @@index([boardId])
  @@index([subjectId])
  @@index([activeSyllabusId])
}

model TuitionSyllabusUpload {
  id            String             @id @default(cuid())
  userId        String
  profileId     String
  sourceType    String
  fileName      String
  mimeType      String
  fileUrl       String
  parseStatus   TuitionParseStatus @default(UPLOADED)
  ocrRawText    String?            @db.LongText
  parseWarnings Json?
  user          User               @relation(fields: [userId], references: [id], onDelete: Cascade)
  profile       TuitionProfile     @relation(fields: [profileId], references: [id], onDelete: Cascade)
  syllabus      TuitionSyllabus?
  createdAt     DateTime           @default(now())
  updatedAt     DateTime           @updatedAt

  @@index([userId, createdAt])
  @@index([profileId, createdAt])
  @@index([parseStatus, updatedAt])
}

model TuitionSyllabus {
  id             String                  @id @default(cuid())
  userId         String
  profileId      String
  uploadId       String?                 @unique
  title          String
  boardId        String?
  classLevel     Int?
  subjectId      String?
  isConfirmed    Boolean                 @default(false)
  user           User                    @relation(fields: [userId], references: [id], onDelete: Cascade)
  profile        TuitionProfile          @relation("ProfileTuitionSyllabi", fields: [profileId], references: [id], onDelete: Cascade)
  upload         TuitionSyllabusUpload?  @relation(fields: [uploadId], references: [id], onDelete: SetNull)
  board          TuitionBoard?           @relation(fields: [boardId], references: [id], onDelete: SetNull)
  subject        TuitionSubject?         @relation(fields: [subjectId], references: [id], onDelete: SetNull)
  chapters       TuitionSyllabusChapter[]
  activeProfiles TuitionProfile[]        @relation("ActiveTuitionSyllabus")
  createdAt      DateTime                @default(now())
  updatedAt      DateTime                @updatedAt

  @@index([userId, createdAt])
  @@index([profileId, createdAt])
  @@index([boardId])
  @@index([subjectId])
}

model TuitionSyllabusChapter {
  id              String               @id @default(cuid())
  syllabusId      String
  name            String
  normalizedName  String
  orderIndex      Int
  sourceText      String?              @db.Text
  isIncluded      Boolean              @default(true)
  syllabus        TuitionSyllabus      @relation(fields: [syllabusId], references: [id], onDelete: Cascade)
  chapterPlans    TuitionChapterPlan[]
  sessions        TuitionSession[]
  progressRecords TuitionProgress[]
  homework        TuitionHomework[]
  lessonCaches    TuitionLessonCache[]
  lessonDoubts    TuitionLessonDoubt[]
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt

  @@unique([syllabusId, orderIndex])
  @@index([syllabusId, normalizedName])
}

model TuitionChapterPlan {
  id                String                @id @default(cuid())
  profileId         String
  syllabusChapterId String
  goalSummary       String?               @db.Text
  estimatedSessions Int?
  recommendedOrder  Int
  profile           TuitionProfile        @relation(fields: [profileId], references: [id], onDelete: Cascade)
  syllabusChapter   TuitionSyllabusChapter @relation(fields: [syllabusChapterId], references: [id], onDelete: Cascade)
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@unique([profileId, syllabusChapterId])
  @@index([profileId, recommendedOrder])
}

model TuitionSession {
  id                String                @id @default(cuid())
  userId            String
  profileId         String
  syllabusChapterId String
  title             String
  speedMode         TuitionSpeedMode      @default(NORMAL)
  difficultyMode    TuitionDifficultyMode @default(MEDIUM)
  responseLanguage  String?
  status            TuitionSessionStatus  @default(ACTIVE)
  user              User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  profile           TuitionProfile        @relation(fields: [profileId], references: [id], onDelete: Cascade)
  syllabusChapter   TuitionSyllabusChapter @relation(fields: [syllabusChapterId], references: [id], onDelete: Cascade)
  messages          TuitionMessage[]
  homework          TuitionHomework[]
  lessonDoubts      TuitionLessonDoubt[]
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@index([userId, syllabusChapterId, updatedAt])
  @@index([profileId, status, updatedAt])
}

model TuitionMessage {
  id                  String         @id @default(cuid())
  sessionId           String
  role                String         @db.VarChar(20)
  content             String         @db.LongText
  contextSnapshotJson Json?
  tokenUsage          Int?
  session             TuitionSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  createdAt           DateTime       @default(now())

  @@index([sessionId, createdAt])
}

model TuitionProgress {
  id                String                @id @default(cuid())
  userId            String
  syllabusChapterId String
  status            TuitionProgressStatus @default(NOT_STARTED)
  completionPercent Int                   @default(0)
  lastSessionId     String?
  lastStudiedAt     DateTime?
  user              User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  syllabusChapter   TuitionSyllabusChapter @relation(fields: [syllabusChapterId], references: [id], onDelete: Cascade)
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@unique([userId, syllabusChapterId])
  @@index([status, lastStudiedAt])
  @@index([lastSessionId])
}

model TuitionHomework {
  id                String                @id @default(cuid())
  userId            String
  profileId         String
  syllabusChapterId String
  sessionId         String?
  title             String
  instructions      String?               @db.Text
  speedMode         TuitionSpeedMode      @default(NORMAL)
  difficultyMode    TuitionDifficultyMode @default(MEDIUM)
  responseLanguage  String?
  status            TuitionHomeworkStatus @default(GENERATED)
  assignmentPayload Json
  sourceContextJson Json?
  user              User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  profile           TuitionProfile        @relation(fields: [profileId], references: [id], onDelete: Cascade)
  syllabusChapter   TuitionSyllabusChapter @relation(fields: [syllabusChapterId], references: [id], onDelete: Cascade)
  session           TuitionSession?       @relation(fields: [sessionId], references: [id], onDelete: SetNull)
  submissions       TuitionHomeworkSubmission[]
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@index([userId, createdAt])
  @@index([profileId, createdAt])
  @@index([syllabusChapterId, createdAt])
  @@index([sessionId])
  @@index([status, updatedAt])
}

model TuitionHomeworkSubmission {
  id                String           @id @default(cuid())
  homeworkId        String
  userId            String
  answerPayload     Json
  notes             String?          @db.Text
  evaluationPayload Json?
  homework          TuitionHomework  @relation(fields: [homeworkId], references: [id], onDelete: Cascade)
  user              User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([homeworkId, createdAt])
  @@index([userId, createdAt])
}

model TuitionLessonCache {
  id                   String                  @id @default(cuid())
  profileId            String?
  syllabusChapterId    String?
  boardName            String?                 @db.VarChar(120)
  classLevel           Int?
  subjectName          String                  @db.VarChar(191)
  topicTitle           String                  @db.VarChar(191)
  explanationLanguage  String                  @db.VarChar(20)
  boardLanguage        String                  @db.VarChar(20)
  voiceLanguage        String                  @db.VarChar(20)
  teachingDepth        String                  @db.VarChar(20)
  speedMode            TuitionSpeedMode
  difficultyMode       TuitionDifficultyMode
  promptType           String                  @db.VarChar(40)
  promptText           String                  @db.Text
  normalizedCacheKey   String                  @unique @db.VarChar(255)
  previousTeachingPhase String?                @db.VarChar(40)
  previousConceptIndex Int?
  assistantPayloadJson Json
  boardPayloadJson     Json?
  generatedAt          DateTime                @default(now())
  lastUsedAt           DateTime?
  hitCount             Int                     @default(0)
  createdAt            DateTime                @default(now())
  updatedAt            DateTime                @updatedAt
  profile              TuitionProfile?         @relation(fields: [profileId], references: [id], onDelete: SetNull)
  syllabusChapter      TuitionSyllabusChapter? @relation(fields: [syllabusChapterId], references: [id], onDelete: SetNull)
  lessonDoubts         TuitionLessonDoubt[]

  @@index([syllabusChapterId, updatedAt])
  @@index([profileId, updatedAt])
  @@index([topicTitle, subjectName, updatedAt])
  @@index([promptType, updatedAt])
}

model TuitionLessonDoubt {
  id                    String                  @id @default(cuid())
  userId                String
  sessionId             String?
  syllabusChapterId     String?
  lessonCacheId         String?
  subjectName           String                  @db.VarChar(191)
  topicTitle            String                  @db.VarChar(191)
  explanationLanguage   String                  @db.VarChar(20)
  boardLanguage         String                  @db.VarChar(20)
  voiceLanguage         String                  @db.VarChar(20)
  teachingDepth         String                  @db.VarChar(20)
  speedMode             TuitionSpeedMode
  difficultyMode        TuitionDifficultyMode
  questionText          String                  @db.LongText
  normalizedQuestionKey String                  @unique @db.VarChar(255)
  answerPayloadJson     Json?
  previousTeachingPhase String?                 @db.VarChar(40)
  previousConceptIndex  Int?
  occurrenceCount       Int                     @default(1)
  importanceScore       Int                     @default(1)
  lastAskedAt           DateTime                @default(now())
  createdAt             DateTime                @default(now())
  updatedAt             DateTime                @updatedAt
  user                  User                    @relation(fields: [userId], references: [id], onDelete: Cascade)
  session               TuitionSession?         @relation(fields: [sessionId], references: [id], onDelete: SetNull)
  syllabusChapter       TuitionSyllabusChapter? @relation(fields: [syllabusChapterId], references: [id], onDelete: SetNull)
  lessonCache           TuitionLessonCache?     @relation(fields: [lessonCacheId], references: [id], onDelete: SetNull)

  @@index([userId, createdAt])
  @@index([sessionId, updatedAt])
  @@index([syllabusChapterId, updatedAt])
  @@index([lessonCacheId, updatedAt])
  @@index([topicTitle, subjectName, updatedAt])
}

model TeacherFeatureFlagRollout {
  id               String   @id @default(cuid())
  flagKey          String   @db.VarChar(120)
  scopeType        String   @db.VarChar(40)
  userId           String?  @db.VarChar(191)
  teacherProfileId String?  @db.VarChar(191)
  isEnabled        Boolean  @default(true)
  note             String?  @db.Text
  createdBy        String?  @db.VarChar(191)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([flagKey, scopeType, isEnabled])
  @@index([userId, flagKey])
  @@index([teacherProfileId, flagKey])
}

model TeacherProfile {
  id                String   @id @default(cuid())
  userId            String   @unique @db.VarChar(191)
  displayName       String   @db.VarChar(191)
  headline          String?  @db.VarChar(191)
  bio               String?  @db.LongText
  status            String   @default("PENDING") @db.VarChar(40)
  canTeachOneToOne  Boolean  @default(true)
  canTeachBatch     Boolean  @default(false)
  subjectsJson      Json?
  boardsJson        Json?
  classLevelsJson   Json?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([status, updatedAt])
}

model TeacherKyc {
  id                   String   @id @default(cuid())
  teacherProfileId     String   @unique @db.VarChar(191)
  legalName            String?  @db.VarChar(191)
  documentType         String?  @db.VarChar(80)
  documentNumberMasked String?  @db.VarChar(80)
  verificationStatus   String   @default("PENDING") @db.VarChar(40)
  documentMetaJson     Json?
  verifiedByAdminId    String?  @db.VarChar(191)
  verifiedAt           DateTime?
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  @@index([verificationStatus, updatedAt])
}

model TeacherPayoutAccount {
  id                 String   @id @default(cuid())
  teacherProfileId   String   @db.VarChar(191)
  accountType        String   @db.VarChar(40)
  accountLabelMasked String   @db.VarChar(191)
  accountMetaJson    Json?
  isVerified         Boolean  @default(false)
  verifiedByAdminId  String?  @db.VarChar(191)
  verifiedAt         DateTime?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@index([teacherProfileId, createdAt])
  @@index([isVerified, updatedAt])
}

model TeacherOffering {
  id            String   @id @default(cuid())
  teacherProfileId String @db.VarChar(191)
  mode          String   @db.VarChar(40)
  title         String   @db.VarChar(191)
  board         String?  @db.VarChar(120)
  classLevel    Int?
  subject       String   @db.VarChar(120)
  billingCycle  String   @db.VarChar(40)
  cyclePrice    Decimal  @db.Decimal(10, 2)
  demoPrice     Decimal  @default(0) @db.Decimal(10, 2)
  batchCapacity Int?
  isPublished   Boolean  @default(false)
  status        String   @default("DRAFT") @db.VarChar(40)
  description   String?  @db.Text
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([teacherProfileId, updatedAt])
  @@index([status, isPublished, updatedAt])
  @@index([subject, board, classLevel])
}

model TeacherOfferingPolicy {
  id                 String   @id @default(cuid())
  teacherOfferingId  String   @unique @db.VarChar(191)
  cancellationPolicy String?  @db.Text
  refundPolicy       String?  @db.Text
  noShowPolicy       String?  @db.Text
  lateJoinPolicy     String?  @db.Text
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
}

model TeacherRequirement {
  id           String   @id @default(cuid())
  studentUserId String  @db.VarChar(191)
  board        String?  @db.VarChar(120)
  classLevel   Int?
  subject      String   @db.VarChar(120)
  modeWanted   String   @db.VarChar(40)
  goals        String?  @db.Text
  status       String   @default("OPEN") @db.VarChar(40)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([studentUserId, updatedAt])
  @@index([status, updatedAt])
}

model TeacherEnrollment {
  id                String   @id @default(cuid())
  studentUserId     String   @db.VarChar(191)
  teacherProfileId  String   @db.VarChar(191)
  teacherOfferingId String   @db.VarChar(191)
  batchId           String?  @db.VarChar(191)
  mode              String   @db.VarChar(40)
  status            String   @default("PENDING") @db.VarChar(40)
  billingCycle      String   @db.VarChar(40)
  currentCycleStart DateTime?
  currentCycleEnd   DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([studentUserId, updatedAt])
  @@index([teacherProfileId, updatedAt])
  @@index([teacherOfferingId, updatedAt])
  @@index([batchId, updatedAt])
  @@index([status, updatedAt])
}

model TeacherBatch {
  id                String   @id @default(cuid())
  teacherProfileId  String   @db.VarChar(191)
  teacherOfferingId String   @db.VarChar(191)
  title             String   @db.VarChar(191)
  capacity          Int?
  status            String   @default("ACTIVE") @db.VarChar(40)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([teacherProfileId, updatedAt])
  @@index([teacherOfferingId, updatedAt])
  @@index([status, updatedAt])
}

model TeacherBatchStudent {
  id            String   @id @default(cuid())
  batchId       String   @db.VarChar(191)
  studentUserId String   @db.VarChar(191)
  enrollmentId  String   @db.VarChar(191)
  status        String   @default("ACTIVE") @db.VarChar(40)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@unique([batchId, studentUserId])
  @@unique([enrollmentId])
  @@index([batchId, updatedAt])
  @@index([studentUserId, updatedAt])
}

model TeacherBoard {
  id               String   @id @default(cuid())
  teacherProfileId String   @db.VarChar(191)
  enrollmentId     String?  @db.VarChar(191)
  batchId          String?  @db.VarChar(191)
  title            String   @db.VarChar(191)
  status           String   @default("ACTIVE") @db.VarChar(40)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([teacherProfileId, updatedAt])
  @@index([enrollmentId, updatedAt])
  @@index([batchId, updatedAt])
}

model TeacherBoardSession {
  id             String   @id @default(cuid())
  teacherBoardId String   @db.VarChar(191)
  startedAt      DateTime @default(now())
  endedAt        DateTime?
  status         String   @default("OPEN") @db.VarChar(40)
  summary        String?  @db.Text
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([teacherBoardId, startedAt])
  @@index([status, updatedAt])
}

model TeacherBoardArtifact {
  id             String   @id @default(cuid())
  teacherBoardId String   @db.VarChar(191)
  sessionId      String?  @db.VarChar(191)
  artifactType   String   @db.VarChar(40)
  title          String?  @db.VarChar(191)
  storageUrl     String?  @db.VarChar(1000)
  mimeType       String?  @db.VarChar(191)
  payloadJson    Json?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([teacherBoardId, updatedAt])
  @@index([sessionId, updatedAt])
  @@index([artifactType, updatedAt])
}

model TeacherContentItem {
  id               String   @id @default(cuid())
  teacherProfileId String   @db.VarChar(191)
  enrollmentId     String?  @db.VarChar(191)
  batchId          String?  @db.VarChar(191)
  contentType      String   @db.VarChar(40)
  title            String   @db.VarChar(191)
  body             String?  @db.LongText
  visibility       String   @default("ENROLLED_ONLY") @db.VarChar(40)
  status           String   @default("PUBLISHED") @db.VarChar(40)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([teacherProfileId, updatedAt])
  @@index([enrollmentId, updatedAt])
  @@index([batchId, updatedAt])
  @@index([contentType, status, updatedAt])
}

model TeacherContentAttachment {
  id           String   @id @default(cuid())
  contentItemId String  @db.VarChar(191)
  fileName     String   @db.VarChar(191)
  mimeType     String   @db.VarChar(191)
  storageUrl   String   @db.VarChar(1000)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([contentItemId, createdAt])
}

model TeacherNotice {
  id               String   @id @default(cuid())
  teacherProfileId String   @db.VarChar(191)
  targetType       String   @db.VarChar(40)
  targetId         String   @db.VarChar(191)
  title            String   @db.VarChar(191)
  body             String   @db.Text
  status           String   @default("PUBLISHED") @db.VarChar(40)
  publishedAt      DateTime?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([teacherProfileId, updatedAt])
  @@index([targetType, targetId, updatedAt])
  @@index([status, publishedAt])
}

model TeacherNoticeRecipient {
  id              String   @id @default(cuid())
  teacherNoticeId  String  @db.VarChar(191)
  studentUserId   String   @db.VarChar(191)
  deliveredAt     DateTime @default(now())
  readAt          DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([teacherNoticeId, studentUserId])
  @@index([studentUserId, updatedAt])
  @@index([readAt])
}

model TeacherOrder {
  id                String   @id @default(cuid())
  studentUserId     String   @db.VarChar(191)
  teacherEnrollmentId String @db.VarChar(191)
  teacherOfferingId String   @db.VarChar(191)
  billingCycle      String   @db.VarChar(40)
  grossAmount       Decimal  @db.Decimal(10, 2)
  platformFeeAmount Decimal  @db.Decimal(10, 2)
  netTeacherAmount  Decimal  @db.Decimal(10, 2)
  paymentStatus     String   @default("PENDING") @db.VarChar(40)
  cycleStart        DateTime?
  cycleEnd          DateTime?
  isDemo            Boolean  @default(false)
  razorpayOrderId   String?  @db.VarChar(191)
  razorpayPaymentId String?  @db.VarChar(191)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([studentUserId, updatedAt])
  @@index([teacherEnrollmentId, updatedAt])
  @@index([paymentStatus, updatedAt])
}

model TeacherBillingCycle {
  id                String   @id @default(cuid())
  teacherEnrollmentId String @db.VarChar(191)
  teacherProfileId  String   @db.VarChar(191)
  cycleType         String   @db.VarChar(40)
  startsOn          DateTime
  endsOn            DateTime
  status            String   @default("OPEN") @db.VarChar(40)
  grossAmount       Decimal  @default(0) @db.Decimal(10, 2)
  platformFeeAmount Decimal  @default(0) @db.Decimal(10, 2)
  netAmount         Decimal  @default(0) @db.Decimal(10, 2)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([teacherEnrollmentId, updatedAt])
  @@index([teacherProfileId, updatedAt])
  @@index([status, endsOn])
}

model TeacherLedgerEntry {
  id               String   @id @default(cuid())
  teacherProfileId String   @db.VarChar(191)
  billingCycleId   String?  @db.VarChar(191)
  teacherOrderId   String?  @db.VarChar(191)
  entryType        String   @db.VarChar(40)
  amount           Decimal  @db.Decimal(10, 2)
  note             String?  @db.Text
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([teacherProfileId, createdAt])
  @@index([billingCycleId, createdAt])
  @@index([teacherOrderId, createdAt])
}

model TeacherPayout {
  id                String   @id @default(cuid())
  teacherProfileId  String   @db.VarChar(191)
  payoutAccountId   String?  @db.VarChar(191)
  cycleCount        Int      @default(0)
  grossAmount       Decimal  @db.Decimal(10, 2)
  platformFeeAmount Decimal  @db.Decimal(10, 2)
  adjustmentAmount  Decimal  @default(0) @db.Decimal(10, 2)
  netAmount         Decimal  @db.Decimal(10, 2)
  status            String   @default("PENDING") @db.VarChar(40)
  releasedAt        DateTime?
  approvedByAdminId String?  @db.VarChar(191)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([teacherProfileId, updatedAt])
  @@index([status, updatedAt])
}

model TeacherModerationFlag {
  id               String   @id @default(cuid())
  teacherProfileId String?  @db.VarChar(191)
  enrollmentId     String?  @db.VarChar(191)
  contentItemId    String?  @db.VarChar(191)
  noticeId         String?  @db.VarChar(191)
  scopeType        String   @db.VarChar(40)
  reason           String   @db.VarChar(191)
  details          String?  @db.Text
  status           String   @default("OPEN") @db.VarChar(40)
  createdBy        String?  @db.VarChar(191)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([status, updatedAt])
  @@index([teacherProfileId, updatedAt])
}

model TeacherAuditLog {
  id          String   @id @default(cuid())
  actorUserId String?  @db.VarChar(191)
  scopeType   String   @db.VarChar(40)
  scopeId     String   @db.VarChar(191)
  action      String   @db.VarChar(120)
  summary     String   @db.Text
  detailsJson Json?
  createdAt   DateTime @default(now())

  @@index([scopeType, scopeId, createdAt])
  @@index([actorUserId, createdAt])
}
